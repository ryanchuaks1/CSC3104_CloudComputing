import 'package:flutter/material.dart';
import 'package:flutter_app/src/ui/views/Login/components/body.dart';

class LoginPage extends StatelessWidget {
  const LoginPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
